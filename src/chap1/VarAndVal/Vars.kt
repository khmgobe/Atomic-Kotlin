package chap1.VarAndVal

fun main() {

    /* 변할 수 있는 값 이라는 이름처럼, 저장된 값은 달라질 수 있다.
       따라서 var 변수를 가변(mutable) 이라고 한다.
     */

    var whole = 11;
    var fractional = 1.4
    var words = "Twas Briling"
    println(whole)
    println(fractional)
    println(words)

}
